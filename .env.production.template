# ==============================================
# PRODUCTION ENVIRONMENT VARIABLES
# ==============================================
# This template shows what environment variables are needed for production.
# DO NOT commit actual values to git!
# Add these to Vercel Environment Variables dashboard.

# ==============================================
# SUPABASE (Database & Storage)
# ==============================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# ==============================================
# GOOGLE CLOUD VISION API (Bottle Detection & Receipt OCR)
# ==============================================
GOOGLE_VISION_API_KEY=your_google_vision_api_key_here

# ==============================================
# PAYPAL PAYOUTS API (Consumer Rebates)
# ==============================================
# IMPORTANT: Use LIVE credentials only! (not sandbox)
# Get from: https://developer.paypal.com/dashboard/applications (LIVE tab)
PAYPAL_CLIENT_ID=your_LIVE_client_id_here
PAYPAL_CLIENT_SECRET=your_LIVE_secret_here
PAYPAL_ENVIRONMENT=live

# DO NOT SET TEST_PAYOUT_AMOUNT in production!
# This should only be used for local/staging testing with penny payouts.
# Omit this variable to use production default ($5.00 rebates).

# ==============================================
# ADMIN DASHBOARD AUTHENTICATION
# ==============================================
# Server-side password (NOT exposed to browser)
# Use a strong, random password!
# Generate one with: openssl rand -base64 32
ADMIN_PASSWORD=your_strong_random_password_here

# ==============================================
# FRAUD PREVENTION
# ==============================================
# Receipt Image Duplicate Detection
# ALWAYS enabled in production
NEXT_PUBLIC_ENABLE_RECEIPT_HASH_CHECK=true

# PayPal Email Rate Limiting
# ALWAYS enabled in production
ENABLE_PAYPAL_EMAIL_RATE_LIMIT=true

# PayPal Email Rate Limit Period (in days)
# How many days must pass before the same email can receive another payout
# Recommended: 30 days
PAYPAL_EMAIL_RATE_LIMIT_DAYS=30

# ==============================================
# PRODUCTION FEATURES
# ==============================================
# Disable test mode features (test receipt creation, bypass bottle detection)
# ALWAYS true in production
NEXT_PUBLIC_DISABLE_TEST_MODE=true

# ==============================================
# OPTIONAL - GEMINI API (Bottle Morph Animation)
# ==============================================
# Only needed if using bottle morphing animation feature
# Get from: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your_gemini_api_key_here

# ==============================================
# VERCEL DEPLOYMENT CHECKLIST
# ==============================================
# 1. Copy all variables above (with real values)
# 2. Go to Vercel project → Settings → Environment Variables
# 3. Add each variable for "Production" environment
# 4. Deploy or redeploy project for changes to take effect
# 5. Test with staging preview deployment first!
# 6. Verify fraud prevention is enabled
# 7. Test PayPal flow with penny payouts first ($0.01)
# 8. Monitor first 24 hours of production usage

# ==============================================
# SECURITY CHECKLIST
# ==============================================
# ✅ PAYPAL_ENVIRONMENT=live (not sandbox)
# ✅ ADMIN_PASSWORD is strong and unique
# ✅ NEXT_PUBLIC_DISABLE_TEST_MODE=true
# ✅ ENABLE_PAYPAL_EMAIL_RATE_LIMIT=true
# ✅ NEXT_PUBLIC_ENABLE_RECEIPT_HASH_CHECK=true
# ✅ TEST_PAYOUT_AMOUNT is NOT set
# ✅ All API keys are production keys (not sandbox/test)
