# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000  # Use https://yourdomain.com in production

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Cloud Vision API
GOOGLE_VISION_API_KEY=your_google_vision_api_key

# Gemini API (for bottle morphing animation)
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key

# PayPal Payouts API
# Get these from: https://developer.paypal.com/dashboard/applications
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret
PAYPAL_ENVIRONMENT=sandbox  # Use 'sandbox' for testing, 'live' for production

# PayPal Webhook ID (for signature verification)
# Create webhook at: https://developer.paypal.com/dashboard/webhooks
# Webhook URL: https://yourdomain.com/api/webhooks/paypal
# Events to subscribe: All PAYMENT.PAYOUTS-ITEM.* events
PAYPAL_WEBHOOK_ID=your_webhook_id

# Admin Dashboard
NEXT_PUBLIC_ADMIN_PASSWORD=your_secure_password_here

# Fraud Prevention Settings
# Receipt Image Duplicate Detection (boolean: 'true' or 'false')
# Set to 'false' to disable receipt hash checking during testing
# Default: enabled (true)
NEXT_PUBLIC_ENABLE_RECEIPT_HASH_CHECK=true

# PayPal Email Rate Limiting (boolean: 'true' or 'false')
# Set to 'false' to disable email rate limiting during testing
# Default: enabled (true)
ENABLE_PAYPAL_EMAIL_RATE_LIMIT=true

# PayPal Email Rate Limit Period (number in days)
# How many days must pass before the same PayPal email can receive another payout
# Default: 30 days
PAYPAL_EMAIL_RATE_LIMIT_DAYS=30
